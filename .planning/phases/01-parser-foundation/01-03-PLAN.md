---
phase: 01-parser-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - resources/views/livewire/pages/inspections/create.blade.php
  - resources/views/livewire/pages/inspections/edit.blade.php
autonomous: false
requirements:
  - PARSE-08
user_setup: []
must_haves:
  truths:
    - "Explicit parse button exists"
    - "Error alert shows on failure"
    - "Parsing is synchronous"
  artifacts:
    - path: "resources/views/livewire/pages/inspections/create.blade.php"
  key_links:
    - from: "Parse Button"
      to: "InspectionParserInterface::parse"
      via: "Livewire action"
---

<objective>
Update UI to trigger parsing synchronously via a button and handle errors.
Purpose: Provides explicit user control over parsing and immediate error feedback.
Output: Updated UI with Parse button and error alert.
</objective>

<context>
@resources/views/livewire/pages/inspections/create.blade.php
@resources/views/livewire/pages/inspections/edit.blade.php
@app/Contracts/InspectionParserInterface.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UI triggers and error alerts</name>
  <files>
    resources/views/livewire/pages/inspections/create.blade.php
    resources/views/livewire/pages/inspections/edit.blade.php
  </files>
  <action>
    1. Remove automatic/debounced parsing in Livewire components.
    2. Add "Parse" button (btn-primary or btn-secondary) below the raw_notes textarea.
    3. Add a DaisyUI alert-warning div that displays only when a parsing error occurs.
    4. Implement the Livewire action to trigger synchronous parsing.
  </action>
  <verify>
    <automated>grep -q "Parse" resources/views/livewire/pages/inspections/create.blade.php</automated>
  </verify>
  <done>UI updated with Parse button and error handling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated inspection parsing flow</what-built>
  <how-to-verify>
    1. Visit inspection create page.
    2. Type notes.
    3. Click "Parse" button.
    4. Verify structure fields populate synchronously.
    5. Trigger a simulated error (by mocking a fake parser) and verify alert-warning shows.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Livewire Client | Sanitized input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Livewire Action | mitigate | Ensure only authorized users can trigger parsing |
</threat_model>

<verification>
UI and functional tests pass.
</verification>

<success_criteria>
- Parse button triggers synchronous parsing.
- Error alert appears on failure.
</success_criteria>
