---
phase: 01-parser-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Contracts/InspectionParserInterface.php
  - app/Services/InspectionParserService.php
  - app/Providers/AppServiceProvider.php
  - app/Jobs/ParseInspectionNotes.php
autonomous: true
requirements:
  - PARSE-01
  - PARSE-08
user_setup: []
must_haves:
  truths:
    - "ParseInspectionNotes job is deleted"
    - "InspectionParserInterface exists and is used"
    - "Service is bound in AppServiceProvider"
  artifacts:
    - path: "app/Contracts/InspectionParserInterface.php"
    - path: "app/Services/InspectionParserService.php"
    - path: "app/Providers/AppServiceProvider.php"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "InspectionParserInterface"
      via: "app()->bind()"
---

<objective>
Refactor the parsing service to use an interface and remove the dead queue job.
Purpose: Decouple the parser from concrete implementation to enable testing fakes.
Output: InspectionParserInterface and updated ServiceProvider.
</objective>

<context>
@app/Services/InspectionParserService.php
@app/Jobs/ParseInspectionNotes.php
@app/Providers/AppServiceProvider.php
@.planning/phases/01-parser-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Interface and update Service</name>
  <files>
    app/Contracts/InspectionParserInterface.php
    app/Services/InspectionParserService.php
  </files>
  <action>
    1. Create app/Contracts/InspectionParserInterface.php with parseRaw(string $rawNotes): array and parse(Inspection $inspection): void.
    2. Update app/Services/InspectionParserService to implement InspectionParserInterface.
  </action>
  <verify>
    <automated>grep -q "implements InspectionParserInterface" app/Services/InspectionParserService.php</automated>
  </verify>
  <done>Interface defined and Service implementation updated to implement it.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead job and update service binding</name>
  <files>
    app/Jobs/ParseInspectionNotes.php
    app/Providers/AppServiceProvider.php
  </files>
  <action>
    1. Delete app/Jobs/ParseInspectionNotes.php.
    2. Update app/Providers/AppServiceProvider to bind InspectionParserInterface to InspectionParserService.
  </action>
  <verify>
    <automated>test ! -f app/Jobs/ParseInspectionNotes.php</automated>
  </verify>
  <done>Dead job deleted and service bound via interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service->OpenAI | Untrusted LLM output |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Service | mitigate | Extract only allow-listed fields from JSON response |
</threat_model>

<verification>
Ensure service binding works and job is removed.
</verification>

<success_criteria>
- ParseInspectionNotes does not exist.
- Service implements interface.
- AppServiceProvider binds interface.
</success_criteria>
