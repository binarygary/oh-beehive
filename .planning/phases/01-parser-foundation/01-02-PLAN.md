---
phase: 01-parser-foundation
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - tests/Unit/FakeInspectionParserService.php
  - tests/Feature/InspectionParserTest.php
autonomous: true
requirements:
  - PARSE-01
user_setup: []
must_haves:
  truths:
    - "No real API calls are made during tests"
    - "InspectionParserService unit tests pass"
  artifacts:
    - path: "tests/Unit/FakeInspectionParserService.php"
  key_links:
    - from: "tests/Unit/FakeInspectionParserService.php"
      to: "InspectionParserInterface"
      via: "implements"
---

<objective>
Enable test isolation by creating a fake parser and verifying parser logic.
Purpose: Ensure we can test parsing logic and Livewire integration without hitting OpenAI API.
Output: FakeInspectionParserService and unit tests for the parsing service.
</objective>

<feature>
  <name>Test Fakes and Service Unit Tests</name>
  <files>tests/Unit/FakeInspectionParserService.php, tests/Feature/InspectionParserTest.php</files>
  <behavior>
    - Service correctly extracts fields from a sample JSON payload.
    - Invalid diseases are filtered out.
    - Short notes are guarded.
    - Fake parser implementation correctly implements the interface.
  </behavior>
  <implementation>Create a mock parser for tests that implements the interface. Write tests that bind the fake in the container.</implementation>
</feature>
